const add = require('./add')

console.log(add(Number(process.argv[2]),3))

var express = require('express');

const app = express();

app.get('/add', )

app.listen(8080); //the server object listens on port 8080